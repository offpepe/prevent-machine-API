
export default class ReturningDataValidationOptions {
    return: {
        company?: boolean;
        manager?: boolean;
    };
    include: {
        company?: boolean,
        unit?: boolean,
    }
}